import { TypographyProps } from '@mui/material';

export interface ButtonEllipseProps {
  bgColor: string;
  variantTypography: TypographyProps['variant'];
  text: string;
  rotate?: string;
  width: string;
  margin?: string;
}

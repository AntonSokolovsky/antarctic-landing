import { createTheme } from '@mui/material';
import { colorThemeOptions } from './colorTheme';
import { typography } from './typography';

export const theme = createTheme(colorThemeOptions, typography);
